import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pluck1983.childminderincometracker',
  appName: 'Childminder Tracker',
  webDir: 'dist'
};

export default config;
